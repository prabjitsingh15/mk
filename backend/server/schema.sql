-- BareAya database schema.
-- The backend creates the tables automatically on startup and seeds the product catalog.
CREATE DATABASE IF NOT EXISTS bareaya CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE bareaya;

-- The API creates these tables and seeds products automatically on startup.
